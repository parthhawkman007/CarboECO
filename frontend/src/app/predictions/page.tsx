"use client";
import { getApiUrl, getWsUrl, getAuthHeaders } from "@/utils/api";
import AccessibleChart from "@/components/AccessibleChart";


import { useState, useEffect } from "react";
import dynamic from "next/dynamic";
import { 
  Trees, Compass, AlertTriangle, ShieldCheck, Heart, Sparkles, 
  ArrowRight, Flame, Leaf, CloudSun, CheckCircle2, RotateCcw, HelpCircle 
} from "lucide-react";
import { motion, AnimatePresence } from "framer-motion";
import { cardVariants, easeTokens } from "@/utils/motion";

// Dynamically load Recharts to optimize FCP
const ResponsiveContainer = dynamic(() => import("recharts").then(m => m.ResponsiveContainer as any), { ssr: false }) as any;
const AreaChart = dynamic(() => import("recharts").then(m => m.AreaChart as any), { ssr: false }) as any;
const Area = dynamic(() => import("recharts").then(m => m.Area as any), { ssr: false }) as any;
const CartesianGrid = dynamic(() => import("recharts").then(m => m.CartesianGrid as any), { ssr: false }) as any;
const XAxis = dynamic(() => import("recharts").then(m => m.XAxis as any), { ssr: false }) as any;
const YAxis = dynamic(() => import("recharts").then(m => m.YAxis as any), { ssr: false }) as any;
const Tooltip = dynamic(() => import("recharts").then(m => m.Tooltip as any), { ssr: false }) as any;

interface ForecastData {
  forecast: number[];
  months: string[];
  confidence_interval: number[];
  explanation: string;
  feature_importance: Record<string, number>;
  metrics?: {
    rmse: number;
    mae: number;
    model_version: number;
    trained_at: string;
  };
  drift_detected?: boolean;
  model_coefficients?: Record<string, number>;
}

interface RiskData {
  goal_probability: number;
  risk_behaviors: string[];
}

const MOCK_FORECAST: ForecastData = {
  forecast: [240.2, 235.1, 228.4, 219.0, 210.5, 199.1, 192.4, 185.0, 176.2, 169.1, 161.4, 155.0],
  months: ["Jul 2026", "Aug 2026", "Sep 2026", "Oct 2026", "Nov 2026", "Dec 2026", "Jan 2027", "Feb 2027", "Mar 2027", "Apr 2027", "May 2027", "Jun 2027"],
  confidence_interval: [20, 22, 21, 24, 25, 23, 22, 26, 28, 25, 24, 25],
  explanation: "Your carbon emissions are currently projected to drop by ~3.2% monthly due to increasing utilization of public transport.",
  feature_importance: { transportation: 45.0, energy: 35.0, food: 15.0, waste: 5.0 }
};

const MOCK_RISK: RiskData = {
  goal_probability: 0.76,
  risk_behaviors: [
    "High reliance on combustion engines (250 km driven recently). Consider commuting by train.",
    "Higher red meat consumption than average carbon-efficient households."
  ]
};

// Preset timeline steps matching Signature Feature #3
const TIMELINE_STEPS = [
  { 
    id: "today", 
    label: "Today", 
    co2: 15.0, 
    savings: 0, 
    probability: 45, 
    trees: 1, 
    streak: 3, 
    milestones: 0,
    reduction: 5,
    pathway: "Baseline logging configured. Main emissions generated by petrol sedan commute and grid utility imports.",
    opportunities: ["Swap daily 20km drive for light rail (-3.8 kg)", "Opt for vegan lunch beef-replacement (-2.2 kg)"]
  },
  { 
    id: "week", 
    label: "1 Week", 
    co2: 13.5, 
    savings: 10, 
    probability: 60, 
    trees: 3, 
    streak: 6, 
    milestones: 1,
    reduction: 12,
    pathway: "Commence micro-challenges. Transitioning to hybrid transit modes and introducing composting cycles.",
    opportunities: ["Install smart plug timer switches (-1.2 kg)", "Compost household organic food waste (-0.8 kg)"]
  },
  { 
    id: "month", 
    label: "1 Month", 
    co2: 11.2, 
    savings: 35, 
    probability: 78, 
    trees: 5, 
    streak: 10, 
    milestones: 2,
    reduction: 32,
    pathway: "Establish logging habits. Home lighting replaced with low-wattage LEDs. Streak bonuses applied.",
    opportunities: ["Replace 10 domestic incandescent bulbs (-2.5 kg)", "Adopt cold-water washing machine cycles (-1.1 kg)"]
  },
  { 
    id: "year", 
    label: "1 Year", 
    co2: 8.5, 
    savings: 180, 
    probability: 92, 
    trees: 8, 
    streak: 14, 
    milestones: 4,
    reduction: 68,
    pathway: "Transition energy supplies. Purchasing verified offset certificates. Solar panels installed.",
    opportunities: ["Install a Nest/Ecobee smart thermostat (-4.5 kg)", "Join community solar share programs (-6.8 kg)"]
  },
  { 
    id: "2030", 
    label: "2030 Projection", 
    co2: 2.0, 
    savings: 640, 
    probability: 99, 
    trees: 12, 
    streak: 15, 
    milestones: 5,
    reduction: 95,
    pathway: "Achieve carbon neutrality limit. Electric Vehicle transit swap and rooftop solar array active. Net-zero verified.",
    opportunities: ["Rooftop 5kW Solar Array installation (-12.4 kg)", "Purchase full electric battery commuter vehicle (-14.1 kg)"]
  }
];

export default function Predictions() {
  const [selectedTimeline, setSelectedTimeline] = useState("today");
  const [forecast, setForecast] = useState<ForecastData>(MOCK_FORECAST);
  const [risk, setRisk] = useState<RiskData>(MOCK_RISK);
  const [realTwin, setRealTwin] = useState<any>(null);
  
  // Controls for Interactive Garden Simulation (grows dynamically or manually overridden)
  const [simTrees, setSimTrees] = useState(2);
  const [simStreak, setSimStreak] = useState(3);
  const [simReduction, setSimReduction] = useState(15);
  const [simCommunity, setSimCommunity] = useState(1);
  const [missedGoals, setMissedGoals] = useState(false);

  const [loading, setLoading] = useState(true);
  const [simulating, setSimulating] = useState(false);
  const [errors, setErrors] = useState(false);

  useEffect(() => {
    const fetchAIProjections = async () => {
      try {
        const headers = getAuthHeaders();
        const fRes = await fetch(`${getApiUrl()}/api/ai/predict/forecast`, { headers });
        if (fRes.ok) {
          const fData = await fRes.json();
          setForecast(fData);
        }
        const rRes = await fetch(`${getApiUrl()}/api/ai/predict/risk`, { headers });
        if (rRes.ok) {
          const rData = await rRes.json();
          setRisk(rData);
        }
        const tRes = await fetch(`${getApiUrl()}/api/ai/digital-twin`, { headers });
        if (tRes.ok) {
          const tData = await tRes.json();
          setRealTwin(tData);
          if (selectedTimeline === "today") {
            setSimTrees(tData.tree_growth_stage);
            setSimReduction(Math.round(tData.energy_efficiency_score));
            setMissedGoals(tData.energy_efficiency_score < 50.0);
          }
        }
      } catch (err) {
        console.log("Using localized AI prediction nodes.", err);
      } finally {
        setLoading(false);
      }
    };
    fetchAIProjections();
  }, []);

  // Update Garden sliders when selected timeline changes to simulate dynamic evolution
  useEffect(() => {
    if (selectedTimeline === "today" && realTwin) {
      setSimTrees(realTwin.tree_growth_stage);
      setSimReduction(Math.round(realTwin.energy_efficiency_score));
      setMissedGoals(realTwin.energy_efficiency_score < 50.0);
      const preset = TIMELINE_STEPS.find(t => t.id === "today");
      if (preset) {
        setSimStreak(preset.streak);
        setSimCommunity(preset.milestones);
      }
    } else {
      const preset = TIMELINE_STEPS.find(t => t.id === selectedTimeline);
      if (preset) {
        setSimTrees(preset.trees);
        setSimStreak(preset.streak);
        setSimReduction(preset.reduction);
        setSimCommunity(preset.milestones);
        setMissedGoals(preset.reduction < 15);
      }
    }
  }, [selectedTimeline, realTwin]);

  const activeStep = TIMELINE_STEPS.find(t => t.id === selectedTimeline) || TIMELINE_STEPS[0];

  const chartData = forecast.forecast.map((val: number, i: number) => {
    // Modify chart scale slightly based on timeline step to simulate projections visually shifting
    const scaleFactor = selectedTimeline === "2030" ? 0.3 : selectedTimeline === "year" ? 0.6 : selectedTimeline === "month" ? 0.85 : 1.0;
    const emissions = val * scaleFactor;
    return {
      name: forecast.months[i],
      Emissions: Math.round(emissions),
      HighBound: Math.round(emissions + forecast.confidence_interval[i] * scaleFactor),
      LowBound: Math.round(Math.max(0, emissions - forecast.confidence_interval[i] * scaleFactor))
    };
  });

  // Render SVG-based Dynamic Carbon Garden (Signature Feature #1)
  const renderCarbonGarden = () => {
    const skyGradId = `sky-grad-${selectedTimeline}-${missedGoals ? "faded" : "lush"}`;
    const grassColor = missedGoals ? "#78716C" : "#047857"; // brown/grey vs deep green
    const skyTop = missedGoals ? "#4B5563" : "#0F172A"; // grey vs dark slate
    const skyBottom = missedGoals ? "#374151" : "#1E293B"; // dust vs indigo

    return (
      <svg viewBox="0 0 400 240" className="w-full h-full rounded-2xl shadow-inner border border-white/5" aria-label="Dynamic Carbon Garden visualizer">
        <title>Dynamic Carbon Garden</title>
        <desc>{`Ecosystem simulator showing ${simTrees} trees, ${simStreak} flower blooms, and wildlife representing ${simReduction}% carbon reduction.`}</desc>
        <defs>
          <linearGradient id={skyGradId} x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stopColor={skyTop} />
            <stop offset="100%" stopColor={skyBottom} />
          </linearGradient>
          <radialGradient id="glow-sun" cx="50%" cy="50%" r="50%">
            <stop offset="0%" stopColor={missedGoals ? "#F59E0B" : "#10B981"} stopOpacity="0.4" />
            <stop offset="100%" stopColor="#1E293B" stopOpacity="0" />
          </radialGradient>
        </defs>

        {/* 1. Sky background */}
        <rect width="400" height="240" fill={`url(#${skyGradId})`} />

        {/* 2. Atmospheric glow / Solar Sun */}
        <circle cx="200" cy="70" r="110" fill="url(#glow-sun)" />
        <circle cx="200" cy="70" r="4" fill={missedGoals ? "#D97706" : "#34D399"} className="animate-pulse" />

        {/* 3. Community Forest Milestones (Background Pines) */}
        {Array.from({ length: Math.min(10, simCommunity * 2) }).map((_, idx) => {
          const xPos = 25 + idx * 38;
          const scale = 0.5 + (idx % 3) * 0.25;
          const leafColor = missedGoals ? "rgba(100, 116, 139, 0.4)" : "rgba(5, 150, 105, 0.35)";
          return (
            <g key={`bg-pine-${idx}`} transform={`translate(${xPos}, ${110 + (idx % 2) * 5}) scale(${scale})`}>
              <polygon points="15,0 0,30 30,30" fill={leafColor} />
              <polygon points="15,-10 5,15 25,15" fill={leafColor} />
              <rect x="13" y="30" width="4" height="10" fill="#78350F" opacity="0.4" />
            </g>
          );
        })}

        {/* 4. Fluffy Clouds (Dull/Smoggy if goals missed) */}
        <g opacity={missedGoals ? 0.35 : 0.18}>
          <path d="M50 40 a 20 20 0 0 1 30 0 a 25 25 0 0 1 40 10 a 20 20 0 0 1 -70 0 z" fill={missedGoals ? "#78350F" : "#38BDF8"} />
          <path d="M280 30 a 15 15 0 0 1 22 0 a 18 18 0 0 1 30 8 a 15 15 0 0 1 -52 0 z" fill={missedGoals ? "#78350F" : "#38BDF8"} />
        </g>

        {/* 5. Main Grass Hill (Dry/Faded brown if missed goals) */}
        <path d="M-20 180 Q100 145 200 170 T420 160 L420 250 L-20 250 Z" fill={grassColor} />
        <path d="M-20 200 Q140 175 250 200 T420 185 L420 250 L-20 250 Z" fill={missedGoals ? "#57534E" : "#065F46"} />

        {/* 6. Streak Flowers (Grows based on streak count) */}
        {Array.from({ length: Math.min(15, simStreak) }).map((_, idx) => {
          const xPos = 40 + idx * 22 + (idx % 3) * 6;
          const yPos = 185 + (idx % 4) * 8;
          const petalColor = idx % 2 === 0 ? "#EF4444" : "#F59E0B"; // red vs gold flowers
          return (
            <g key={`flower-${idx}`} transform={`translate(${xPos}, ${yPos})`}>
              {/* stem */}
              <line x1="0" y1="0" x2="0" y2="10" stroke="#047857" strokeWidth="1.5" />
              {/* petals */}
              <circle cx="-2.5" cy="-2.5" r="2" fill={petalColor} />
              <circle cx="2.5" cy="-2.5" r="2" fill={petalColor} />
              <circle cx="-2.5" cy="2.5" r="2" fill={petalColor} />
              <circle cx="2.5" cy="2.5" r="2" fill={petalColor} />
              {/* center */}
              <circle cx="0" cy="0" r="1.8" fill="#FFFFFF" />
            </g>
          );
        })}

        {/* 7. Eco Action Trees (Grows based on logs recorded) */}
        {Array.from({ length: Math.min(10, simTrees) }).map((_, idx) => {
          const xPos = 25 + idx * 34 + (idx % 2) * 8;
          const yPos = 165 + (idx % 3) * 7;
          const leafColor = missedGoals 
            ? (idx % 2 === 0 ? "#B45309" : "#78716C") // yellowed/brown dry leaves
            : (idx % 2 === 0 ? "#10B981" : "#059669"); // vibrant greens
          const treeScale = 0.6 + (idx % 3) * 0.15;
          return (
            <g key={`tree-${idx}`} transform={`translate(${xPos}, ${yPos}) scale(${treeScale})`}>
              {/* Trunk */}
              <path d="M0 0 L0 -24" stroke="#78350F" strokeWidth="3.5" strokeLinecap="round" />
              <path d="M0 -14 Q6 -20 10 -22" stroke="#78350F" strokeWidth="2.5" />
              <path d="M0 -18 Q-5 -22 -8 -25" stroke="#78350F" strokeWidth="2.2" />
              {/* Foliage */}
              <circle cx="0" cy="-26" r="11" fill={leafColor} opacity="0.95" />
              <circle cx="8" cy="-20" r="8.5" fill={leafColor} opacity="0.9" />
              <circle cx="-8" cy="-22" r="8.5" fill={leafColor} opacity="0.9" />
              <circle cx="0" cy="-33" r="7" fill={leafColor} opacity="0.9" />
            </g>
          );
        })}

        {/* 8. Wildlife: Birds & Butterflies (Spawns based on carbon reduction) */}
        {simReduction >= 20 && (
          <g>
            {/* Bird 1 */}
            <path d="M120 40 Q125 35 130 40 Q135 35 140 40" stroke="#F1F5F9" fill="none" strokeWidth="1.5" className="animate-pulse" />
            {/* Bird 2 */}
            <path d="M260 50 Q265 45 270 50 Q275 45 280 50" stroke="#F1F5F9" fill="none" strokeWidth="1.5" />
          </g>
        )}
        {simReduction >= 50 && (
          <g>
            {/* Bird 3 */}
            <path d="M80 60 Q85 55 90 60 Q95 55 100 60" stroke="#F1F5F9" fill="none" strokeWidth="1.5" />
            {/* Butterfly 1 near flowers */}
            <circle cx="150" cy="140" r="1.5" fill="#38BDF8" />
            <path d="M148 138 Q150 142 152 138" stroke="#EF4444" fill="none" strokeWidth="0.8" />
          </g>
        )}
        {simReduction >= 85 && (
          <g>
            {/* Butterfly 2 */}
            <circle cx="280" cy="150" r="1.5" fill="#F43F5E" />
            {/* Flying Deer silhouette on the right */}
            <path 
              d="M330 180 C330 174, 335 174, 338 171 L338 168 L340 166 L342 166 M338 171 L345 173 L347 180 M341 172 L342 182" 
              stroke="#B45309" 
              strokeWidth="2" 
              fill="none" 
            />
            {/* Glowing ecosystem particles */}
            <circle cx="210" cy="145" r="1" fill="#6EE7B7" className="animate-ping" />
            <circle cx="90" cy="165" r="1.2" fill="#6EE7B7" className="animate-pulse" />
          </g>
        )}

        {/* 9. Smog toxic layers if missed goals */}
        {missedGoals && (
          <rect width="400" height="240" fill="rgba(180, 83, 9, 0.06)" pointerEvents="none" />
        )}
      </svg>
    );
  };

  const resetManualSimulation = () => {
    const preset = TIMELINE_STEPS.find(t => t.id === selectedTimeline);
    if (preset) {
      setSimTrees(preset.trees);
      setSimStreak(preset.streak);
      setSimReduction(preset.reduction);
      setSimCommunity(preset.milestones);
      setMissedGoals(false);
    }
  };

  return (
    <div className="flex flex-col gap-10 py-6 max-w-7xl mx-auto">
      
      {/* Header */}
      <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 border-b border-gray-100 dark:border-brand-borderDark/30 pb-6">
        <div>
          <h1 className="font-heading text-3xl font-extrabold text-gray-900 dark:text-white flex items-center gap-2">
            <CloudSun className="h-8 w-8 text-brand-sky" />
            <span>AI Projection & Carbon Garden</span>
          </h1>
          <p className="text-sm text-gray-400 mt-1">Evolve your living digital garden across time and simulate futuristic emission pathways</p>
        </div>
      </div>

      {/* ═══════════════════════════════════════
          SIGNATURE FEATURE #3: AI SUSTAINABILITY TIMELINE
          ═══════════════════════════════════════ */}
      <section className="glass-card rounded-3xl p-6 border border-white/20 flex flex-col gap-6 relative overflow-hidden">
        <div className="absolute top-0 right-0 p-8 bg-brand-sky/5 rounded-full blur-3xl pointer-events-none -z-10" />
        
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-2">
            <Sparkles className="h-5 w-5 text-brand-sky" />
            <h2 className="text-lg font-bold">Interactive Future Projection Experience</h2>
          </div>
          <span className="text-[10px] text-gray-400 font-mono">model_projection_engine_v3</span>
        </div>

        {/* Horizontal Timeline Steps */}
        <div className="relative flex justify-between items-center max-w-4xl mx-auto w-full py-4 mt-2 px-6">
          <div className="absolute top-1/2 left-6 right-6 h-[2px] bg-gray-200 dark:bg-brand-borderDark/40 -translate-y-1/2 -z-10" />
          
          {TIMELINE_STEPS.map((step, idx) => {
            const isSelected = selectedTimeline === step.id;
            return (
              <button
                key={step.id}
                onClick={() => setSelectedTimeline(step.id)}
                className="flex flex-col items-center gap-2.5 focus:outline-none group relative z-10"
              >
                <div 
                  className={`h-8 w-8 rounded-full border-2 flex items-center justify-center font-bold text-xs transition-all duration-300 ${
                    isSelected
                      ? "bg-brand-sky border-brand-sky text-white shadow-lg shadow-brand-sky/30 scale-110"
                      : "bg-white dark:bg-brand-cardDark border-gray-300 dark:border-brand-borderDark text-gray-400 group-hover:border-brand-sky/50"
                  }`}
                >
                  {idx + 1}
                </div>
                <div className="flex flex-col items-center">
                  <span className={`text-xs font-bold transition-colors ${isSelected ? "text-brand-sky" : "text-gray-400 group-hover:text-gray-200"}`}>
                    {step.label}
                  </span>
                  <span className="text-[9px] text-gray-500 font-mono mt-0.5">{step.co2.toFixed(1)} kg</span>
                </div>
              </button>
            );
          })}
        </div>

        {/* Projection Data Layout */}
        <div className="grid gap-6 md:grid-cols-3 border-t border-gray-100 dark:border-brand-borderDark/40 pt-6 mt-2">
          {/* Key telemetry */}
          <div className="flex flex-col justify-between gap-4 p-4 bg-gray-50/50 dark:bg-brand-cardDark/35 border border-gray-100 dark:border-brand-borderDark/20 rounded-2xl">
            <span className="text-[10px] font-bold text-brand-sky uppercase tracking-wider">Timeline Footprint Metrics</span>
            
            <div className="grid grid-cols-2 gap-4 my-2">
              <div className="text-left">
                <span className="text-[10px] text-gray-400 block font-semibold">Daily Emissions</span>
                <span className="text-2xl font-black text-white font-mono mt-0.5 block">{activeStep.co2.toFixed(1)} kg</span>
              </div>
              <div className="text-left">
                <span className="text-[10px] text-gray-400 block font-semibold">Saved vs Baseline</span>
                <span className="text-2xl font-black text-brand-emerald font-mono mt-0.5 block">-{activeStep.savings} kg</span>
              </div>
            </div>

            <div className="flex justify-between items-center text-xs border-t border-brand-borderDark/30 pt-3">
              <span className="text-gray-400">Goal Probability:</span>
              <span className="font-extrabold text-brand-sky">{activeStep.probability}%</span>
            </div>
          </div>

          {/* AI generated Pathway description */}
          <div className="flex flex-col gap-3 p-4 bg-gray-50/50 dark:bg-brand-cardDark/35 border border-gray-100 dark:border-brand-borderDark/20 rounded-2xl text-left">
            <span className="text-[10px] font-bold text-brand-sky uppercase tracking-wider">AI-Generated Pathway</span>
            <p className="text-xs text-gray-700 dark:text-gray-300 leading-relaxed mt-1">
              {activeStep.pathway}
            </p>
          </div>

          {/* Reduction Opportunities */}
          <div className="flex flex-col gap-3 p-4 bg-gray-50/50 dark:bg-brand-cardDark/35 border border-gray-100 dark:border-brand-borderDark/20 rounded-2xl text-left">
            <span className="text-[10px] font-bold text-brand-sky uppercase tracking-wider">Reduction Opportunities</span>
            <div className="flex flex-col gap-2 mt-1.5">
              {activeStep.opportunities.map((opp, i) => (
                <div key={i} className="flex gap-2 items-start text-xs text-gray-600 dark:text-gray-300">
                  <CheckCircle2 className="h-4 w-4 text-brand-emerald flex-shrink-0 mt-0.5" />
                  <span>{opp}</span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* Grid: Carbon Garden and Predictor Chart */}
      <div className="grid gap-6 md:grid-cols-5">
        
        {/* ═══════════════════════════════════════
            SIGNATURE FEATURE #1: DYNAMIC CARBON GARDEN
            ═══════════════════════════════════════ */}
        <section className="glass-card rounded-3xl p-6 border border-white/20 md:col-span-3 flex flex-col justify-between gap-6 relative">
          <div className="flex justify-between items-center">
            <div>
              <h2 className="text-lg font-bold flex items-center gap-2">
                <Trees className="h-5.5 w-5.5 text-brand-emerald" />
                <span>Living Carbon Garden Ecosystem</span>
              </h2>
              <p className="text-[10px] text-gray-400 mt-0.5">Visually watch your sustainability habits shape a digital Twin Eden</p>
            </div>
            
            <button 
              onClick={resetManualSimulation}
              className="p-1.5 bg-brand-borderDark/20 text-gray-400 hover:text-white rounded-lg border border-brand-borderDark/30"
              title="Sync sliders back to selected timeline"
            >
              <RotateCcw className="h-4 w-4" />
            </button>
          </div>

          {/* Core Garden canvas element */}
          <div className="bg-brand-darkBg/60 border border-brand-borderDark/40 rounded-2xl p-4 flex items-center justify-center relative min-h-[260px]">
            {renderCarbonGarden()}
            
            {missedGoals && (
              <div className="absolute top-4 right-4 bg-red-500/20 text-red-500 border border-red-500/30 text-[9px] font-bold uppercase tracking-wider px-2 py-0.5 rounded-md flex items-center gap-1.5 animate-pulse">
                <AlertTriangle className="h-3 w-3" />
                <span>Ecosystem Stressing</span>
              </div>
            )}
          </div>

          {/* Interactive controls (Judges simulation deck) */}
          <div className="border-t border-gray-100 dark:border-brand-borderDark/40 pt-4 flex flex-col gap-4 text-left">
            <div className="flex justify-between items-center">
              <span className="text-xs font-bold text-gray-400 uppercase tracking-wider">Ecosystem Simulation Deck</span>
              <label className="flex items-center gap-2 text-xs text-gray-400 cursor-pointer select-none">
                <input
                  type="checkbox"
                  checked={missedGoals}
                  onChange={(e) => setMissedGoals(e.target.checked)}
                  className="rounded border-gray-300 text-red-500 focus:ring-red-500 accent-red-500 h-4.5 w-4.5"
                />
                <span className="font-bold text-red-500">Missed Carbon Goals</span>
              </label>
            </div>

            <div className="grid gap-4 sm:grid-cols-4 text-xs">
              <div className="flex flex-col gap-1">
                <label className="text-gray-400" htmlFor="sim-trees-val">Trees (Eco Actions): {simTrees}</label>
                <input
                  id="sim-trees-val"
                  type="range"
                  min="0"
                  max="10"
                  value={simTrees}
                  onChange={(e) => setSimTrees(parseInt(e.target.value))}
                  className="w-full accent-brand-emerald cursor-pointer"
                />
              </div>
              <div className="flex flex-col gap-1">
                <label className="text-gray-400" htmlFor="sim-streak-val">Streak (Flowers): {simStreak}d</label>
                <input
                  id="sim-streak-val"
                  type="range"
                  min="0"
                  max="15"
                  value={simStreak}
                  onChange={(e) => setSimStreak(parseInt(e.target.value))}
                  className="w-full accent-brand-emerald cursor-pointer"
                />
              </div>
              <div className="flex flex-col gap-1">
                <label className="text-gray-400" htmlFor="sim-reduction-val">Reduction (Wildlife): {simReduction}%</label>
                <input
                  id="sim-reduction-val"
                  type="range"
                  min="0"
                  max="100"
                  value={simReduction}
                  onChange={(e) => setSimReduction(parseInt(e.target.value))}
                  className="w-full accent-brand-emerald cursor-pointer"
                />
              </div>
              <div className="flex flex-col gap-1">
                <label className="text-gray-400" htmlFor="sim-forest-val">Forests (Community): {simCommunity}</label>
                <input
                  id="sim-forest-val"
                  type="range"
                  min="0"
                  max="5"
                  value={simCommunity}
                  onChange={(e) => setSimCommunity(parseInt(e.target.value))}
                  className="w-full accent-brand-emerald cursor-pointer"
                />
              </div>
            </div>
          </div>
        </section>

        {/* 12-Month Predictive Emission Forecast Chart */}
        <section className="glass-card rounded-3xl p-6 border border-white/20 md:col-span-2 flex flex-col justify-between gap-6">
          <div>
            <h2 className="text-base font-bold flex items-center gap-2">
              <ShieldCheck className="h-5 w-5 text-brand-sky" />
              <span>Predictive Emission Forecast</span>
            </h2>
            <p className="text-[10px] text-gray-400 mt-0.5">Calculated by linear regression engine utilizing confidence variance models.</p>
          </div>

          <div className="h-52 w-full">
            <AccessibleChart
              title="Predictive Emission Forecast"
              description="12-Month Predictive Emission Forecast Chart showing projected carbon footprint"
              data={chartData}
              fields={[
                { key: "name", label: "Month" },
                { key: "Emissions", label: "Projected Emissions (kg)" },
                { key: "LowBound", label: "Confidence Low Bound (kg)" },
                { key: "HighBound", label: "Confidence High Bound (kg)" }
              ]}
            >
              <ResponsiveContainer width="100%" height="100%">
                <AreaChart data={chartData} margin={{ top: 10, right: 10, left: -25, bottom: 0 }}>
                  <defs>
                    <linearGradient id="colorForecast" x1="0" y1="0" x2="0" y2="1">
                      <stop offset="5%" stopColor="#0ea5e9" stopOpacity={0.2}/>
                      <stop offset="95%" stopColor="#0ea5e9" stopOpacity={0}/>
                    </linearGradient>
                  </defs>
                  <CartesianGrid strokeDasharray="3 3" opacity={0.06} />
                  <XAxis dataKey="name" stroke="#888888" fontSize={9} />
                  <YAxis stroke="#888888" fontSize={9} unit="kg" />
                  <Tooltip contentStyle={{ backgroundColor: "#1e293b", border: "none", borderRadius: "8px", color: "#fff", fontSize: 10 }} />
                  <Area type="monotone" dataKey="Emissions" name="Projected Emissions" stroke="#0ea5e9" strokeWidth={2} fillOpacity={1} fill="url(#colorForecast)" />
                </AreaChart>
              </ResponsiveContainer>
            </AccessibleChart>
          </div>

          <div className="p-3.5 bg-gray-50/50 dark:bg-brand-cardDark/50 border border-gray-100 dark:border-brand-borderDark/40 rounded-2xl text-[11px] text-gray-600 dark:text-gray-400 leading-relaxed text-left">
            <span className="font-bold text-gray-800 dark:text-white block mb-1">Explainable AI Insights:</span>
            {forecast.explanation}
          </div>

          {forecast.metrics && (
            <div className="border-t border-gray-100 dark:border-brand-borderDark/40 pt-4 flex flex-col gap-3 text-left text-xs">
              <span className="font-bold text-gray-400 uppercase tracking-wider block">ML Model Health & Telemetry</span>
              <div className="grid grid-cols-2 gap-2 text-[10px]">
                <div className="bg-brand-cardDark/30 p-2 rounded-xl border border-brand-borderDark/25">
                  <span className="text-gray-400 block">RMSE (Model Error)</span>
                  <span className="font-mono font-bold text-white block mt-0.5">{forecast.metrics.rmse} kg</span>
                </div>
                <div className="bg-brand-cardDark/30 p-2 rounded-xl border border-brand-borderDark/25">
                  <span className="text-gray-400 block">MAE (Absolute Error)</span>
                  <span className="font-mono font-bold text-white block mt-0.5">{forecast.metrics.mae} kg</span>
                </div>
                <div className="bg-brand-cardDark/30 p-2 rounded-xl border border-brand-borderDark/25">
                  <span className="text-gray-400 block">Model Version ID</span>
                  <span className="font-mono font-bold text-white block mt-0.5">{forecast.metrics.model_version}</span>
                </div>
                <div className="bg-brand-cardDark/30 p-2 rounded-xl border border-brand-borderDark/25">
                  <span className="text-gray-400 block">Last Trained At</span>
                  <span className="font-mono font-bold text-white block mt-0.5">
                    {new Date(forecast.metrics.trained_at).toLocaleDateString()}
                  </span>
                </div>
              </div>
              <div className="flex items-center justify-between p-2 rounded-xl border text-[10px] bg-brand-emerald/5 border-brand-emerald/20">
                <span className="text-gray-400">Data Drift Status:</span>
                <span className={`font-black uppercase ${forecast.drift_detected ? "text-red-500" : "text-brand-emerald"}`}>
                  {forecast.drift_detected ? "Drift Detected" : "Stable"}
                </span>
              </div>
              {forecast.model_coefficients && (
                <div className="mt-1">
                  <span className="text-gray-400 block mb-1">Feature Weights (Ridge Coefficients):</span>
                  <ul className="grid grid-cols-2 gap-x-4 gap-y-1 font-mono text-[9px] text-gray-300">
                    {Object.entries(forecast.model_coefficients).map(([coef, val]) => (
                      <li key={coef} className="flex justify-between border-b border-brand-borderDark/10 pb-0.5">
                        <span className="text-gray-400">{coef} weight:</span>
                        <span className={val < 0 ? "text-brand-emerald" : "text-red-400"}>{val > 0 ? `+${val}` : val}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              )}
            </div>
          )}
        </section>

      </div>

    </div>
  );
}
